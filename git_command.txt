git status                      # current repo ka status check karna
git add testing.py              # specific file ko staging me add karna
git rm --cached testing.py     # file ko untrack karna (local me rahegi)
git add .                       # sab files ko staging me add karna
git commit -m "added testing files"   # changes ko save (commit) karna
git config --global user.name "nupurgurnule"   # username set karna
git config --global user.email "nupurgurnule01@gmail.com"  # email set karna
git commit -m "added testing files"   # dubara commit karna (after config)
git restore testing.py         # file ko last commit state pe lana
notepad testing.py             # file open karke edit karna
git status                     # changes check karna
git restore testing.py         # changes discard karna
git status                     # final clean status check